export class Choices {
  name: string;
  img: string;
  className: string;

  constructor(name: string, img: string, className: string) {
    this.name = name;
    this.img = img;
    this.className = className;
  }
}